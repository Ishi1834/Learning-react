import Button from "./Button";

const Header = () => {
  return (
    <header className="header">
      <h1>Reading List</h1>
      <Button color="#055052" text="Add" />
    </header>
  );
};

export default Header;
